Act as a Principal Software Engineer executing a proactive code stabilization pass. Autonomously scan the codebase to find and fix:

    Real logic bugs, race conditions, and unhandled promise rejections.

    Stale closures, state desynchronizations, and missing effect dependencies.

    Silent error paths, dead code, and duplicated logic.

    Resilience gaps where bad or null data could crash the application.

Implement fixes directly without introducing breaking changes, new features, or subjective cosmetic edits. If a critical fix strictly requires a major breaking change, stop and outline the trade-offs first before proceeding.
