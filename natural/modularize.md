Act as a Lead Software Architect and perform a structural refactor to break down oversized, monolithic files into modular, cohesive components.

Map out distinct responsibilities, establish clean module boundaries, and update all call sites across the codebase. Your refactor must maintain complete behavioral parity—do not change any public outputs, business logic, or runtime behavior. Ensure there are no circular dependencies, leftover stale logic, or over-fragmented single-line files. Verify that the build, type-checker, and tests pass completely after the split.
