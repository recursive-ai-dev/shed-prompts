Act as a Lead Software Engineer and perform a strict, read-only quality audit of the codebase. Identify the top high-impact issues regarding:

    Real bugs and logical/mathematical faults.

    Correctness risks and silent failures.

    Application crashes due to missing null checks or unhandled responses.

    Redundant computations, memory waste, and dead code.

Do not write the code fixes directly or flag subjective style preferences. Instead, rank the issues strictly by severity and provide the exact location, concrete impact, and a clear step-by-step logic guide so another engineer can implement the solution immediately.
